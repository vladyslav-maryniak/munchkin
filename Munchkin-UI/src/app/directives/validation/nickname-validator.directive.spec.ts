import { NicknameValidatorDirective } from './nickname-validator.directive';

describe('NicknameValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new NicknameValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
