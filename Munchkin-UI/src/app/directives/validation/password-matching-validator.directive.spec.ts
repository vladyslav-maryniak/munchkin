import { PasswordMatchingValidatorDirective } from './password-matching-validator.directive';

describe('PasswordMatchingValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new PasswordMatchingValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
