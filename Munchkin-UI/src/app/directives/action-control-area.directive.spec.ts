import { ActionControlAreaDirective } from './action-control-area.directive';

describe('GameStateActionButtonDirective', () => {
  it('should create an instance', () => {
    const directive = new ActionControlAreaDirective();
    expect(directive).toBeTruthy();
  });
});
