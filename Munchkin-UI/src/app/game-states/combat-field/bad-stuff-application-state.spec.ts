import { BadStuffApplicationState } from './bad-stuff-application-state';

describe('BadStuffApplicationState', () => {
  it('should create an instance', () => {
    expect(new BadStuffApplicationState()).toBeTruthy();
  });
});
