import { CombatCompletionState } from './combat-completion-state';

describe('CombatCompletionState', () => {
  it('should create an instance', () => {
    expect(new CombatCompletionState()).toBeTruthy();
  });
});
