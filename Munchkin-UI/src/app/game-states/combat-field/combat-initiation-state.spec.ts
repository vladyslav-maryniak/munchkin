import { CombatInitiationState } from './combat-initiation-state';

describe('CombatInitiation', () => {
  it('should create an instance', () => {
    expect(new CombatInitiationState()).toBeTruthy();
  });
});
